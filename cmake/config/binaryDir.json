{
    "version": 7,
    "configurePresets": [
        {
            "name": "config-binaryDir",
            "hidden": true,
            "binaryDir": "${sourceDir}/build/${presetName}",
            "cacheVariables": {
                "CMAKE_INSTALL_PREFIX": "${sourceDir}/install/${presetName}/installed"
            }
        }
    ]
}